<%= form_for @contact_form do |f| %>
  <%= f.label :message %>
  <%= f.text_area :message, required: true %>

  <%= f.hidden_field :lot_address, :value => land.address %>

  <%= f.submit "Submit", class: 'form-control btn btn-success' %>
<% end %>

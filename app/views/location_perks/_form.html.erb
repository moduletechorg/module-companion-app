<%= form_with(model: location_perk, local: true) do |form| %>
  <% if location_perk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location_perk.errors.count, "error") %> prohibited this location_perk from being saved:</h2>

      <ul>
      <% location_perk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

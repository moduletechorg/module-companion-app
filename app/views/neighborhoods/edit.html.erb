<!-- only for admin -->
<% content_for :admin_console do %>
  <%= link_to "Remove Neighborhood", neighborhood_path(@neighborhood),
      method: :delete,
      class: 'btn btn-danger' %>
<% end %>

<div class="container">
  <div class="row">
    <h2 class="text-center col">Editing <%= @neighborhood.name %></h2>
  </div>

  <br />

  <%= render "form" %>


</div>
